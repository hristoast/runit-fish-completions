# runit fish completions

`fish` tab-completions for runit and friends.  Actually just `sv` right now.

## Usage

Copy `sv.fish` to `$HOME/.config/fish/completions` or `/usr/share/fish/completions`, then tab away!
